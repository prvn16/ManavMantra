%event.hasListener   
%    Determine if listeners exist for specified event
%    
%    tf = event.hasListener(obj,'EventName') returns true if listeners
%    exist for the specified event on the object obj.
%    If obj is an array, event.hasListener returns a logical array the same
%    size as obj.
%
%    See also ADDLISTENER, NOTIFY, event.EventData, EVENT.PROPLISTENER,
%    EVENT.LISTENER

%   Copyright 2015 The MathWorks, Inc. 
%   Built-in function.