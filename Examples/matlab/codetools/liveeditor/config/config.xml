<?xml version="1.0"?>

<fop version="1.0">
    
    <!-- Source resolution in dpi (dots/pixels per inch) for determining the size of pixels in SVG and bitmap images, default: 72dpi -->
    <source-resolution>72</source-resolution>
    <!-- Target resolution in dpi (dots/pixels per inch) for specifying the target resolution for generated bitmaps, default: 72dpi -->
    <target-resolution>72</target-resolution>

    <!-- Default page-height and page-width, in case
         value is specified as auto -->
    <default-page-settings height="11in" width="8.50in"/>
    <strict-validation>false</strict-validation>
    <!-- Information for specific renderers -->
    <!-- Uses renderer mime type for renderers -->
    <renderers>
        <renderer mime="application/pdf">
            <filterList>
                <value>flate</value>
            </filterList>
            <fonts>
                <font kerning="yes" embed-url="ARIALUNI.TTF">
                    <font-triplet name="Arial Unicode MS" style="normal" weight="normal"/>
                    <font-triplet name="Arial Unicode MS" style="normal" weight="400"/>
                    <font-triplet name="Arial Unicode MS" style="normal" weight="700"/>
                    <font-triplet name="Arial Unicode MS" style="italic" weight="normal"/>
                    <font-triplet name="Arial Unicode MS" style="italic" weight="400"/>
                    <font-triplet name="Arial Unicode MS" style="italic" weight="700"/>
                </font>
                <auto-detect/>
            </fonts>
        </renderer>
    </renderers>
</fop>

