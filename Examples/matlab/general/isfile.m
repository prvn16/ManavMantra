%ISFILE Determine if the input path points to a file
%   TF = ISFILE(PATH) returns true if PATH is a file and false otherwise.
%
%   PATH can include a relative path, but the relative path must be in the current folder. 
%   Otherwise, PATH must include a full path.
%
%   PATH can be a string array, a character vector, or a cell array of
%   character vectors.

%   See also ISFOLDER and DIR.

%   Copyright 2016 The MathWorks, Inc.

%   Package: commandfuns
%   Built-in function.
