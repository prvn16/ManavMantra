% G1133654: WHICH looks for variables in the workspace 
% prior to functions, classes, etc, so if a symbol has the same name as a
% local variable, WHICH considers that symbol a variable.
% Calling WHICH in a separate function may isolate many variables in
% function cacheWhich().
% 10^(-18) is effective zero in terms of possibility, which requries
% log(10^18)/log(26+10+1) = 12 random letters, digits, or underscores.
function klno6phn_9faskf_na = callWhich(asm_foyan_knaouie8)
    klno6phn_9faskf_na = which(asm_foyan_knaouie8);
end